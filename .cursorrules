// Vue 3 Composition API .cursorrules

// Vue 3 Composition API best practices

const vue3CompositionApiBestPractices = [
  "Ask follow-up questions if you think this will improve the quality of your answer before generating",
  "Use setup() function for component logic",
  "Utilize ref and reactive for reactive state",
  "Implement computed properties with computed()",
  "Use watch and watchEffect for side effects",
  "Implement lifecycle hooks with onMounted, onUpdated, etc.",
  "Utilize provide/inject for dependency injection",
  "Implement logic in external, well-organized composables whenever possible",
  "Avoid custom CSS and style tags when possible",
  "Components that the router loads are to be put into view",
  "Components should be self-contained, and reasonably small",
  "Follow DRY, but also YAGNI, and do not over-engineer"
];

// Folder structure

const folderStructure = `
backend/
  backend/ # django app
    authapi/ # backend auth
    learnapi/ # back for video and learning data
frontend/ vue3 vite app
`;

// Additional instructions

const additionalInstructions = `
THIS APP USES VITE/VUE3/TS FOR THE FRONTEND AND DJANGO FOR THE BACKEND!!
- for vue:
    - Use TypeScript for type safety, by defining types in types.ts
    - Implement proper props and emits definitions
    - Implement proper error handling
    - Follow Vue 3 style guide and naming conventions
    - Use Vite for fast development and building
    - Unit-test all complex logic
    - Use Tailwind.CSS and Daisy UI for css and interface logic
    - Use dexie.js to manage a local database to handle data
- for django
  - Use latest Django REST Framework 
  - Use Python best practices
`;
