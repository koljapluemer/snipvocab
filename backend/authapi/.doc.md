A Django REST API responsible for basic auth functionality: login, logout, register.

- Uses `JWT`
- Inspired by [this tutorial](https://tomdekan.com/articles/vue-auth) (especially the `forms.py`, unifying email and username)